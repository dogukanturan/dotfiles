if [[ "$OSTYPE" == "darwin"* ]]; then
  alias ll="ls -lhrtG"
  alias l="ls -lahrtG"
else
  alias ll="ls -lhrt --color=tty"
  alias l="ls -lahrt --color=tty"
fi

alias docker-rm-dangling='docker image prune -f'
alias vim="nvim"
