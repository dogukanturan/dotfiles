# aliases
alias l="ls -lahrt --color=tty"

# abbr misc
abbr -a fish-reload "source ~/.config/fish/config.fish"

# abbr kubernetes
abbr -a kctx "kubectl config get-contexts -o name"

# abbr git
abbr -a gst "git status"
